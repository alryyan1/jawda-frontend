import { TranslationProvider } from '@/components/providers/TranslationProvider';

function App() {
  return (
    <TranslationProvider>
      {/* Your existing app content */}
    </TranslationProvider>
  );
}

export default App;
